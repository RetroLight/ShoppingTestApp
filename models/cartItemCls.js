class CartItemCls {
    constructor(itemImg, quantity, productPrice, productTitle, sum) {
        this.itemImg = itemImg,
        this.quantity = quantity;
        this.productPrice = productPrice;
        this.productTitle = productTitle;
        this.sum = sum;
    }
}

export default CartItemCls
